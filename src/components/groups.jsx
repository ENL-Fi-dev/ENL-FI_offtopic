import React, {} from 'react';

import TgGroup from './tgGroup';

const Groups = (props) => {
  return <section>
    <p>'TABS' selector here (ALL, ENL, CF, K18)</p>
    <p>tg groups will be listed here</p>
    <TgGroup/>
    <TgGroup/>
    <TgGroup/>
  </section>;
};

export default Groups;